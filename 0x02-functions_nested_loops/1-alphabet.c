#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0
 */
void print_alphabet(void)
{
    int alpha = 'a';
    for (alpha = 'a'; alpha <= 'z'; alpha++)
    putchar(alpha);
    return (0);
}
