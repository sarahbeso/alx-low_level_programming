#include "main.h"
#include <stdio.h>

/**
 * _abs - check the code
 *@n: takes in integer type input for function
 * Return: Always 0.
 */
int _abs(int n)
{
if (n < 0)
n = (-1) * n;
_putchar('n');
return (0);
}
