#include <stdio.h>
#include <time.h>
#include <stdlib.h>
/**
* main - Entry point
 * Description: print a value of n if it is greater
 * than 5 or equal 0 or less than 6 and not zero.
 * Return: 0 (success)
 */
int main(void)
{
	char ch = 'a';
while (ch <= 'z')

{putchar(ch);
ch++;
}
	putchar('\n');
	return (0);

}
